whoami
Used this command to verify the current logged-in username for my account.

groups or id
Executed this to see the user groups my account belongs to, which determines my system access levels.

echo $SHELL
Checked the $SHELL environment variable to identify the current command-line shell I am operating in (e.g., bash or zsh).

pwd
The pwd command confirmed my present working directory, ensuring I know exactly where I am in the file system.

ls -la
Used ls -la to list all files and directories in my workspace, including hidden files, along with their permissions.

ping -c 4 google.com
Sent four ICMP echo requests to a public domain to verify that the Linux system has active outbound network connectivity.
